function solution(num_list) {
    return num_list.reverse();
}
// const solution = (num_list) => num_list.reverse();

// 이 코드는 원본을 훼손하지 않고 복사해서 배열을 생성함
// const solution = (num_list) => [...num_list].reverse();
